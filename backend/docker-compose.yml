version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: bazaStronyKNAlgo
    restart: always
    environment:
      POSTGRES_DB: stronaKNAlgo
      POSTGRES_USER:
      POSTGRES_PASSWORD:
    ports:
      - "5432:5432"

volumes:
  db_data:

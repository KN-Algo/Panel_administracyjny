package algo.module;

/**
 * Defines the logical group of a person for sorting and displaying sections in the "Team" tab.
 */
public enum TeamRole {
    BOARD,
    MEMBER,
    SUPERVISOR,
    MASCOT
}
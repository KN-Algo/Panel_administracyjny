package algo.module;

/** Defines available post types used in the system. */
public enum PostType {
  /** Standard post type. */
  STANDARD,
  /** News post type. */
  NEWS,
  /** Temporary standard post type. */
  TEMP_STANDARD,
  /** Temporary news post type. */
  TEMP_NEWS
}
